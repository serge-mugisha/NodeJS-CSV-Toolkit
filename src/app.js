/**
 * This Program reads data from a csv file, parses it,
 * and store the filtered records in an array of objects which is later outputed on the screen
 * Provides CRUD functionalities on the parsed data through the terminal
 * File: App.js - main program entry
 * @author Serge Mugisha
 * @version 1.0.0
 */

const { menuSystem } = require('./views/mainView.js')

// Call the menu system
menuSystem()





